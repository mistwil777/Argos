# ============================================
# OpenWebMCP - Docker Compose
# Local Development Environment
# ============================================

services:
  # ============================================
  # PostgreSQL 16 - Main Database
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: openwebmcp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-openwebmcp}
      POSTGRES_USER: ${POSTGRES_USER:-openwebmcp}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=en_US.UTF-8"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
      - ./database/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-openwebmcp} -d ${POSTGRES_DB:-openwebmcp}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - openwebmcp-network

  # ============================================
  # MCP Server - FastAPI + JSON-RPC
  # ============================================
  mcp-server:
    build:
      context: .
      dockerfile: mcp_server/Dockerfile
    container_name: openwebmcp-mcp-server
    restart: unless-stopped
    environment:
      - ENVIRONMENT=development
      - DATABASE_URL=postgresql://${POSTGRES_USER:-openwebmcp}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-openwebmcp}
      - LANCEDB_PATH=/data/lancedb
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - LOG_LEVEL=INFO
      # LLM Provider Configuration
      - LLM_PROVIDER=${LLM_PROVIDER:-aws}
      - DEFAULT_DIGEST_MODEL=${DEFAULT_DIGEST_MODEL:-us.amazon.nova-pro-v1:0}
      # AWS Bedrock Configuration
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_BEDROCK_MODEL=${AWS_BEDROCK_MODEL:-us.amazon.nova-pro-v1:0}
      - DEFAULT_CLASSIFICATION_MODEL=${DEFAULT_CLASSIFICATION_MODEL:-us.amazon.nova-pro-v1:0}
      - DEFAULT_DIGEST_MODEL=${DEFAULT_DIGEST_MODEL:-us.amazon.nova-pro-v1:0}
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      # Cache directories for SentenceTransformer
      - TRANSFORMERS_CACHE=/data/lancedb/transformers_cache
      - SENTENCE_TRANSFORMERS_HOME=/data/lancedb/sentence_transformers
      - HF_HOME=/data/lancedb/huggingface
      # Playwright
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
      - SEARXNG_URL=http://searxng:8080
    volumes:
      - ./mcp_server:/app/mcp_server:ro
      - ./config:/app/config:ro
      - ./docs:/app/docs:ro
      - ./database:/app/database:ro
      - ./scripts:/app/scripts:ro
      - .:/app/project_root:ro
      - lancedb_data:/data/lancedb
      - ./logs:/app/logs
      - playwright_browsers:/ms-playwright
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/bin/python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - openwebmcp-network

  # ============================================
  # SearXNG - Moteur de recherche agrégateur
  # ============================================
  searxng:
    image: searxng/searxng:latest
    container_name: openwebmcp-searxng
    restart: unless-stopped
    environment:
      - SEARXNG_SECRET_KEY=openwebmcp-searxng-secret-2026
      - SEARXNG_BASE_URL=http://searxng:8080
    volumes:
      - ./config/searxng:/etc/searxng:rw
    ports:
      - "8888:8080"
    networks:
      - openwebmcp-network
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID

# ============================================
# Networks
# ============================================
networks:
  openwebmcp-network:
    driver: bridge
    name: openwebmcp-network

# ============================================
# Volumes
# ============================================
volumes:
  postgres_data:
    name: openwebmcp-postgres-data
  lancedb_data:
    name: openwebmcp-lancedb-data
  playwright_browsers:
    name: openwebmcp-playwright-browsers
